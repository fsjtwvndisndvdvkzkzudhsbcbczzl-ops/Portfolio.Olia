# Olia.Shuyu 作品集视觉规范

## 1. 核心原则

- 气质：理性、可信、前沿，但保留设计者的审美判断。
- 版式：电脑端一屏为主，优先保证阅读顺序和字号可读性。
- 颜色：白色为主表面，冷灰建立层级，钴蓝只用于强调和可交互内容。
- 容器：尽量使用留白、对齐和分隔线建立层级，不滥用卡片。

## 2. 字体

- 主字体：`Avenir Next` → `Avenir` → `Helvetica Neue` → `PingFang SC` → `Microsoft YaHei` → 无衬线字体。
- 等宽字体：`SFMono-Regular` 仅用于年份、页码、编号和英文小标签。
- 中文正文优先使用 400/500；标题使用 600；导航和重要按钮可使用 600/700。

## 3. 字号体系

全站最小字号为 **10px**，最大字号为 **60px**。

| 级别 | 字号 | 用途 |
|---|---:|---|
| Micro | 10px | 编号、年份、英文小标签、状态 |
| Caption | 11px | 辅助说明、次要链接 |
| Label | 12px | 字段名、简介、紧凑详情 |
| Body Small | 13px | 卡片正文、项目摘要 |
| Body | 14px | 默认正文 |
| Body Large | 16px | 引导语、重要摘要 |
| Subtitle | 18px | 次级标题、岗位或奖项强调 |
| Title Small | 20px | 列表项标题 |
| Card Title | 24px | 核心卡片标题 |
| Section Small | 28px | 较小的板块标题 |
| Section | 28–38px | 首页主板块标题 |
| Page Title | 32–44px | 独立页标题 |
| Display | 42–60px | 首页英文主标题，全站唯一展示级 |

### 排版约束

- 10px 不承载长句或核心职责。
- 正文行高使用 1.5–1.7；大标题行高使用 1.05–1.2。
- 24px 以上标题使用轻微负字距；10px 英文小标签使用正字距。
- 同一区域原则上不超过四个字号层级。

## 4. 颜色体系

| 语义 | 颜色 | 用途 |
|---|---|---|
| Canvas | `#E9EDF4` | 网页外层背景 |
| Surface | `#FFFFFF` | 主页面、内容表面 |
| Surface Muted | `#F4F7FB` | 弱强调区域 |
| Text | `#151A24` | 标题和主正文 |
| Text Muted | `#667084` | 辅助正文 |
| Text Faint | `#98A3B5` | 占位、页码分母等非关键信息 |
| Border | `#D8DFE9` | 分隔线和弱边界 |
| Accent | `#2467E8` | 当前状态、链接、AI 入口、重点数据 |
| Accent Strong | `#174BAC` | 可交互文字和深色强调 |
| Accent Soft | `#E8F2FF` | 浅色强调背景 |

### 颜色约束

- 一个页面只使用一个交互强调色：Accent Blue。
- 主正文使用 Text，辅助说明使用 Text Muted；Text Faint 不用于重要信息。
- 分隔线优先使用 Border，透明度只用来降低强度。
- 项目图像、证书和文创效果图可保留内容自身颜色，但不改变界面导航与文字颜色体系。

## 5. 交互统一

- Hover：200–300ms，优先改变颜色、透明度或位移 2–4px。
- 大型卡片切换：540–720ms，使用统一的自然减速曲线。
- Active：按钮可缩放至 0.97–0.98，但不使用弹跳动效。
- Focus：键盘焦点使用 2px Accent 轮廓，不允许隐藏。
- 页面切换和内容切换不改变固定信息区的高度。
- 项目主类别使用编号文字索引，不使用胶囊式 Tab；类别通过点击切换，项目通过悬停或键盘焦点展开。
- 项目分类切换时先收拢旧卡片，再进入新卡片；保留每个分类上次展开的项目。
- 项目全景默认显示“精选”，不在一屏中同时放置全部项目。

## 6. 自检清单

- [ ] 是否仍有小于 10px 或大于 60px 的字号？
- [ ] 正文是否被错用为 Micro 字号？
- [ ] 是否出现了蓝色以外的第二个界面强调色？
- [ ] 弱灰色是否承载了关键结论？
- [ ] 同一板块是否超过四个字号层级？
- [ ] 新组件是否使用 `styles.css` 中的字号和颜色变量？
